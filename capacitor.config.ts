import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ema-5093478-u1',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
